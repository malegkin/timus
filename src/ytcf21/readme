Сверка подсистем
Все языки	Python 2.7	Python 3.6
Ограничение времени	1 секунда	6 секунд	6 секунд
Ограничение памяти	64Mb	256Mb	256Mb
Ввод	стандартный ввод или input.txt
Вывод	стандартный вывод или output.txt
Заказы из общей очереди согласно некоторому правилу распределяются между двумя подсистемами.

Чтобы удостовериться, что ни один заказ не был утерян или обработан более одного раза, а также что заказы внутри каждой подсистемы были обработаны в порядке поступления, применяется следующая схема.

Каждому заказу случайным образом присваивается код — символ некоторого алфавита [a..zA..Z0-9]. Таким образом формируется строка, соответствующая входной очереди.

Каждая подсистема на выходе также формирует строку из кодов заказа, которые она обработала.

Будем считать, что система находится в хорошем состоянии, если исходную строку можно получить путем чередования символов из строк подсистем 1 и 2.

Напишите программу, которая будет по входным строкам производить сверку и возвращать YES, если сверка успешна, и NO в противном случае.

Пример

Подсистема 1: «bbabba»

Подсистема 2: «bcacabbcc»

Исходная строка: «bbcacbabbcabbac»

Результат: YES

Ограничения:

Длина строки сверки 1 ≤ L ≤ 10000

Формат ввода
Три строки представляющие собой ответы подсистем 1 и 2 и строку для сверки

Формат вывода
Строка YES, если сверка успешна и NO в противном случае

Пример 1
Ввод	Вывод
bbabba
bcacabbccc
bbcacbabbcabbac
NO
Пример 2
Ввод	Вывод
bbabba
bcacabbcc
bbcacbabbcabbac

